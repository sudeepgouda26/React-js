import { createContext } from "react"


export const theme = {
    dark: {
        BackGroundClour: "black"
    },
    light: {
        BackGroundClour: "white"
    }
};

export let ThemeContextApi = createContext(theme);



